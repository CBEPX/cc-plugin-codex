interface:
  display_name: "Claude Code Rescue"
  short_description: "Args: --background, --wait, --resume, --resume-last, --fresh, --write, --model <model|opus|sonnet|haiku|fable>, --effort <low|medium|high|xhigh|max>, --timeout-ms <ms>, --prompt-file <path>, [task text]. Delegate substantial work through the built-in rescue subagent path."
